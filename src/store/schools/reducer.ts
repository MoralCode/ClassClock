const initialState = {};

export default function reduce(state = initialState, action = {}) {
    // switch (action.type) {
    //     default:
    //         return state;
    // }
    return state;
}
